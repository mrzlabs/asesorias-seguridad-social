{
  "name": "asesorias-api-proxy",
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy a Apps Script",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  }
}
